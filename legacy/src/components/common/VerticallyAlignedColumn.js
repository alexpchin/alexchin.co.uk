import styled from 'styled-components';
import { Columns } from 'react-bulma-components';

const VerticallyAlignedColumn = styled(Columns.Column)`
  display: flex;
  align-items: center;
  justify-content: center;
`;

export { VerticallyAlignedColumn };
