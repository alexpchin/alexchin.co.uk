import styled from 'styled-components';
import { Heading as BulmaHeading } from 'react-bulma-components';
import { white } from '../../constants/colors';

const Heading = styled(BulmaHeading)`
  color: ${white};
`;

export { Heading };
